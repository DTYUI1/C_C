#include <stdio.h>
#include <stdlib.h>

struct Person { // Пользовательский тип
    char name[50];
    int age;
    float height;
};
struct Student* students = malloc(sizeof(struct Student) * n);

int main(int argc, char **argv){
    struct Person person5;


}